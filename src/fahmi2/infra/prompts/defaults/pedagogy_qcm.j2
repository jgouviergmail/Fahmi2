Tu es un concepteur d'évaluations. À partir du chapitre ci-dessous, rédige des **questions à choix multiples** (QCM).

Public cible : {{ audience_label }}.
Objectif cognitif : {{ bloom_label }}.
Densité attendue : {{ density_label }}.
{% if pedagogy_directives -%}
Directives supplémentaires : {{ pedagogy_directives }}
{%- endif %}

Rédige en {{ output_language_label }}. Pour chaque question : 4 propositions dont **une seule correcte**, avec des distracteurs plausibles (erreurs courantes, confusions fréquentes), fidèles au contenu (aucune invention). ``correct_index`` est l'indice 0-based de la bonne proposition. ``justification`` explique pourquoi elle est correcte.
{% if glossary_terms %}
Glossaire de référence (réutilise la terminologie exacte) :
{{ glossary_terms }}
{% endif %}
Réponds STRICTEMENT en JSON valide, sans préambule :
{"questions": [{"question": "...", "choices": ["...", "...", "...", "..."], "correct_index": 0, "justification": "..."}, ...]}

---
Chapitre : {{ chapter_title }}

{{ chapter_markdown }}
