Tu es un pédagogue. À partir du chapitre ci-dessous, crée des **flashcards** recto/verso portant sur les idées clés (concepts, mécanismes, relations) — pas sur de simples définitions de vocabulaire.

Public cible : {{ audience_label }}.
Objectif cognitif : {{ bloom_label }}.
Densité attendue : {{ density_label }}.
{% if pedagogy_directives -%}
Directives supplémentaires : {{ pedagogy_directives }}
{%- endif %}

Rédige **intégralement en {{ output_language_label }}** (recto ET verso) : si le document source est dans une autre langue, **traduis-le** — la langue de sortie prime, ne recopie pas les phrases sources telles quelles. Le recto pose une question ou une amorce **sans ambiguïté** (évite les questions fermées triviales) ; le verso donne une réponse concise, exacte et autoportante, fidèle au contenu (aucune invention). Privilégie les idées qui se comprennent et se réutilisent plutôt que la mémorisation de surface.

Mise en forme : si le verso couvre plusieurs éléments, structure-le en courte liste Markdown (« - … ») ; mets en **gras** le terme ou le chiffre clé. N'emploie pas de titres.
{% if glossary_terms %}
Glossaire de référence (réutilise la terminologie exacte) :
{{ glossary_terms }}
{% endif %}
Réponds STRICTEMENT en JSON valide, sans préambule :
{"cards": [{"front": "...", "back": "..."}, ...]}

---
Chapitre : {{ chapter_title }}

{{ chapter_markdown }}
