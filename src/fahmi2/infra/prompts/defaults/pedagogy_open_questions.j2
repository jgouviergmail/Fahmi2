Tu es un concepteur d'évaluations. À partir du chapitre ci-dessous, rédige des **questions ouvertes**.

Public cible : {{ audience_label }}.
Objectif cognitif : {{ bloom_label }}.
Densité attendue : {{ density_label }}.
{% if pedagogy_directives -%}
Directives supplémentaires : {{ pedagogy_directives }}
{%- endif %}

Rédige en {{ output_language_label }}. Chaque question appelle une réponse développée et mobilise l'**analyse, l'explication ou la comparaison** (évite la simple restitution). ``expected_points`` liste 2 à 5 éléments attendus **distincts et vérifiables** (critères de correction), fidèles au contenu (aucune invention).

Mise en forme : chaque élément attendu est une puce courte et autoportante (une idée par point) ; **gras** inline possible, sans saut de ligne dans un point.
{% if glossary_terms %}
Glossaire de référence (réutilise la terminologie exacte) :
{{ glossary_terms }}
{% endif %}
Réponds STRICTEMENT en JSON valide, sans préambule :
{"questions": [{"question": "...", "expected_points": ["...", "..."]}, ...]}

---
Chapitre : {{ chapter_title }}

{{ chapter_markdown }}
